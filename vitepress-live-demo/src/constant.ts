/**
 * Vue ComponentName for `live-demo`
 */
export const LiveDemoComponentName = 'LiveDemo';

export type LiveDemoFile = {
  filePath: string;
  name: string;
  codeStr: string;
  htmlStr?: string;
  language: string;
};
